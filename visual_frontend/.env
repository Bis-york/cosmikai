# --- Required service URL ---
VITE_API_BASE_URL=https://api.your-domain.com  # used to fetch star/detection data

# --- App base path (only change if serving under a subpath) ---
VITE_BASE=/

# --- Local dev/preview (use different ports from base-frontend) ---
VITE_HOST=0.0.0.0
VITE_PORT=5174
VITE_STRICT_PORT=true
VITE_PREVIEW_HOST=0.0.0.0
VITE_PREVIEW_PORT=4174
