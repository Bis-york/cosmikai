version: "3.9"

services:
  visual-frontend:
    build:
      context: ./visual_frontend
      dockerfile: Dockerfile
    container_name: cosmikai-visual-frontend
    environment:
      VITE_API_BASE_URL: api.flyingwaffle.ca
    ports:
      - "5173:5173"
