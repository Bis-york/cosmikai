# --- Required service URLs ---
VITE_API_BASE_URL=https://api.your-domain.com
VITE_VISUAL_BASE_URL=https://visual.your-domain.com   # used to open the 3D explorer

# --- App base path (only change if serving under a subpath) ---
VITE_BASE=/

# --- Local dev/preview (choose ports that don't clash with the other app) ---
VITE_HOST=0.0.0.0
VITE_PORT=5180
VITE_STRICT_PORT=true
VITE_PREVIEW_HOST=0.0.0.0
VITE_PREVIEW_PORT=5280
